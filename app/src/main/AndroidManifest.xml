<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />


    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <application
        android:allowBackup="true"
        android:banner="@mipmap/ic_launcher"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.REWatchParty">
        <activity
            android:name=".CreateOrJoinWPActivity"
            android:exported="false" />
<!--        <activity-->
<!--            android:name=".CreateRoomActivity"-->
<!--            android:exported="false"-->
<!--            android:label="@string/title_activity_create_join_room"-->
<!--            android:theme="@style/Theme.REWatchParty" />-->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WaitRoomActivity" />
        <activity android:name=".YouTubeActivity" />
        <activity android:name=".WatchMovieActivity" />
        <activity android:name=".RoomDetailsActivity" />
        <activity android:name=".JoinRoomActivity" />
        <activity android:name=".CreateRoomActivity" />




    </application>

</manifest>